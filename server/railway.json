{
  "$schema": "https://schema.railway.app/railway-json-schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "pnpm db:migrate:deploy",
    "startCommand": "node dist/index.js",
    "healthcheckPath": "/health"
  }
}
